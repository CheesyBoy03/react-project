import React from 'react';
import style from './Main.module.css';

const Main = (props) => {
    return(
        <div className={style.banner}>
            <h1>Main Page</h1>
        </div>
    );
};

export default Main;